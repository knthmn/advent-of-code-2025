[project]
name = "advent-of-code-2025"
version = "0.1.0"
description = "Python solutions for Advent of Code 2025"
readme = "README.md"
requires-python = ">=3.13"
dependencies = [
    "pytest>=9.0.1",
]

[dependency-groups]
dev = [
    "ruff>=0.14.7",
]
